pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
openpyxl>=3.1
streamlit>=1.30
plotly>=5.18
# Optional — only needed for the semantic (embeddings) matcher and NL search bonus.
# The app runs fully without these; it falls back to TF-IDF.
# sentence-transformers>=2.2
